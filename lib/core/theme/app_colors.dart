import 'package:flutter/material.dart';

class AppColors {
  // Primary Brand Colors
  static const Color primary = Color(0xFFC6F432); // Lime Green
  static const Color primaryDark = Color(0xFFA4CC28); // Darker Lime

  // Background Colors
  static const Color background = Color(0xFF000000); // Pure Black
  static const Color surface = Color(0xFF1C1C1E); // Dark Grey (Cards)
  static const Color surfaceLight = Color(
    0xFFF4F6F0,
  ); // Off-White (Bottom Sheet)
  static const Color surfaceDark = Color(0xFF1C1C1E); // Dark Grey (Cards)

  // Accent Colors
  static const Color accent = Color(0xFFFF9500); // Orange accent

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF); // White on Dark
  static const Color textSecondary = Color(0xFFB3B3B3); // Grey on Dark
  static const Color textInversePrimary = Color(
    0xFF000000,
  ); // Black on Light/Lime
  static const Color textInverseSecondary = Color(0xFF666666); // Grey on Light

  // Status Colors
  static const Color error = Color(0xFFFF453A);
  static const Color success = Color(0xFF32D74B);
  static const Color warning = Color(0xFFFFD60A);

  // Overlay Colors
  static const Color overlay = Color(0xAA000000);
}
